info:
  name: Check authentication call
  type: http
  seq: 5

http:
  method: GET
  url: GET https://dummyjson.com/auth/me
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
