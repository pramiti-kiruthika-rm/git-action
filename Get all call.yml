info:
  name: Get all call
  type: http
  seq: 1

http:
  method: GET
  url: "https://dummyjson.com/products?name: =Get all call&type=http"
  params:
    - name: "name: "
      value: Get all call
      type: query
    - name: type
      value: http
      type: query
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
